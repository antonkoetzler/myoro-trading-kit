//! Crypto backtest using shared harness.

use anyhow::Result;

pub struct CryptoBacktestRunner;

impl CryptoBacktestRunner {
    pub fn run(_fixture_path: &str) -> Result<()> {
        Ok(())
    }
}
