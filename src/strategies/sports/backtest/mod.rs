//! Sports backtest: historical fixtures + Polymarket snapshots.

use anyhow::Result;

pub struct SportsBacktestRunner;

impl SportsBacktestRunner {
    pub fn run(_fixture_path: &str) -> Result<()> {
        Ok(())
    }
}
