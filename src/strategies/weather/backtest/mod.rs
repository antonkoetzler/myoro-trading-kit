//! Weather backtest: historical forecast vs resolution.

use anyhow::Result;

pub struct WeatherBacktestRunner;

impl WeatherBacktestRunner {
    pub fn run(_fixture_path: &str) -> Result<()> {
        Ok(())
    }
}
